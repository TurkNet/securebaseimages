name: Build and Push Docker Images

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker images
        run: |
          if git rev-parse --verify HEAD^ >/dev/null 2>&1; then
            diff_ref="HEAD^"
          else
            diff_ref="HEAD"
          fi

          changed_dirs=$(git diff --name-only $diff_ref HEAD | grep -E '^rte|^sdk|^3rd-party' | grep 'Dockerfile' | xargs -I {} dirname {} | uniq)

          for dir in $changed_dirs; do
            dockerfile="$dir/Dockerfile"
            base_dir=$(echo $dir | cut -d'/' -f1)
            sub_dir=$(basename $dir)

            # Adjust image naming for 3rd-party directory
            if [ "$base_dir" = "3rd-party" ]; then
              image_name="$sub_dir"
            else
              image_name="$sub_dir-$base_dir"
            fi

            version=$(cat $dir/version)

            full_tag=turknethub/$image_name:$version-${{ github.run_number }}

            docker buildx build --platform=linux/amd64,linux/arm64 --tag $full_tag -f $dockerfile $dir --push
          done
